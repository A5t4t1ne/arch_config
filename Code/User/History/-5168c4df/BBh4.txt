# find_package(OpenSSL REQUIRED)

# # libsodium
# find_package(PkgConfig REQUIRED)
# pkg_check_modules(LIBSODIUM REQUIRED libsodium)
# include_directories(${LIBSODIUM_INCLUDE_DIRS})

# add_executable(pw_manager main.c crypto.c files.c password.c)

# target_include_directories(pw_manager PUBLIC include)
# target_link_libraries(pw_manager ${LIBSODIUM_LIBRARIES} ${X11_LIBRARIES})
# target_link_libraries(pw_manager OpenSSL::Crypto)


find_package(OpenSSL REQUIRED)

add_executable(pw_manager main.c crypto.c files.c password.c)

target_include_directories(pw_manager PUBLIC include)

# Ensure that the linker uses the static library
set(CMAKE_FIND_LIBRARY_SUFFIXES ".a")
find_library(LIBSODIUM_LIBRARY NAMES sodium PATHS /usr/local/lib NO_DEFAULT_PATH)

target_link_libraries(pw_manager ${LIBSODIUM_LIBRARY} ${X11_LIBRARIES})
target_link_libraries(pw_manager OpenSSL::Crypto)

# Reset the suffixes to the default
set(CMAKE_FIND_LIBRARY_SUFFIXES ".so" ".a")